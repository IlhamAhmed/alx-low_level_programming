#include "holberton.h"
#include <stdio.h>
/**
 *print_to_98 - prints upto 98
 *@n: takes int
 *Return: void
 */
void print_to_98(int n)
{
	int i;

	for (i = n; i <= 98; i++)
	{
		if (i == 98)
		{
			printf("%d", i);
		}
		else
		{
			printf("%d, ", i);
		}
	}
}
