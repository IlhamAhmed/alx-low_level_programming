#include <stdio.h>
/**
 *main - prints upper and lower case letters
 *Return: 0 success
 */
int main(void)
{
	int i;

	for (i = 97; i < 123; i++)
	{
		putchar(i);
	}

	int j;

	for(j = 65; j < 97; j++)
	{
		putchar(j);
	}
	putchar(10);
	return (0);
}
