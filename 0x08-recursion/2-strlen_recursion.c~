#include "holberton.h"
/**
 *_strlen_recursion - returns the lenght of a string
 *@s: the string
 *Return: the length
 */
int _strlen_recursion(char *s)
{
	int i = 0;

	if (*s == '\0')
	{
		return (i);
	}
	i++;
	_strlen_recursion(s + 1);
}
