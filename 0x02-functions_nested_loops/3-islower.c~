#include "holberton.h"
/**
 * _islower - checks if a letter is in lowercase
 * @c: takes int  as parameter
 */
int _islower(int c)
{
	if (c > 96 && c < 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
